using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rotate_Tower : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
       // this.transform.localRotation = GameObject.Find("ARSceneCamera").transform.localRotation;
       // Debug.LogError(GameObject.Find("ARSceneCamera").transform.rotation);
    }
}
